import { makeStyles } from '@mui/styles';

export const accessRequestActionsRendererStyles = makeStyles(() => ({
    root: {

    },
    circularProgress: {
        verticalAlign: 'middle',
    }
}));